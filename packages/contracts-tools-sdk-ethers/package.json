{
  "name": "@sandbox/contracts-tools-sdk-ethers",
  "description": "Module simplify smart contract interactions and multicall3 aggregation on the Ethereum blockchain and other EVM-compatible networks.",
  "version": "1.0.0",
  "license": "MIT",
  "main": "src/index.ts",
  "files": ["lib"],
  "scripts": {
    "prepublish": "$npm_execpath build",
    "build": "tsc --build  tsconfig.build.json",
    "test": "vitest run --coverage --exclude **/local/**",
    "test:local": "vitest run --coverage --include **/local/**"
  },
  "peerDependencies": {
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "ethers": "^6.13.5",
    "typescript": "^5.7.2",
    "vitest": "^3.0.5"
  },
  "publishConfig": {
    "main": "lib/index.js",
    "types": "lib/index.d.ts"
  }
}
